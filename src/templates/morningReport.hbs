<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Morning Attendance Report</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      font-size: 14px;
      line-height: 1.6;
      color: #333;
      background: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      max-width: 680px;
      margin: 40px auto;
      background: #ffffff;
      border: 1px solid #ddd;
      border-radius: 4px;
      overflow: hidden;
    }
    .header {
      background: #1a1a2e;
      padding: 24px 32px;
    }
    .header h1 {
      margin: 0;
      color: #ffffff;
      font-size: 18px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }
    .header p {
      margin: 4px 0 0 0;
      color: #aaa;
      font-size: 12px;
    }
    .body {
      padding: 32px;
    }
    .body p {
      margin: 0 0 16px 0;
      color: #444;
    }
    .all-clear {
      background: #f6fef9;
      border: 1px solid #b7e4c7;
      border-radius: 3px;
      padding: 20px 24px;
      margin: 16px 0 24px 0;
      color: #1a6e3a;
      font-size: 14px;
    }
    .all-clear strong {
      display: block;
      font-size: 15px;
      margin-bottom: 4px;
    }
    .summary-row {
      display: flex;
      gap: 16px;
      margin: 0 0 32px 0;
    }
    .summary-card {
      flex: 1;
      border: 1px solid #ddd;
      border-radius: 3px;
      padding: 16px 20px;
      text-align: center;
    }
    .summary-card .label {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #888;
      margin-bottom: 8px;
    }
    .summary-card .value {
      font-size: 32px;
      font-weight: 700;
      color: #1a1a2e;
      line-height: 1;
    }
    .section-title {
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #888;
      border-bottom: 1px solid #eee;
      padding-bottom: 8px;
      margin: 0 0 16px 0;
    }
    .data-table {
      width: 100%;
      border-collapse: collapse;
      margin: 0 0 32px 0;
      font-size: 13px;
    }
    .data-table th {
      text-align: left;
      padding: 8px 12px;
      background: #f5f5f5;
      color: #555;
      font-weight: 600;
      border-bottom: 1px solid #ddd;
    }
    .data-table td {
      padding: 10px 12px;
      border-bottom: 1px solid #eee;
      color: #333;
    }
    .data-table tr:last-child td {
      border-bottom: none;
    }
    .badge {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 2px;
      font-size: 11px;
      font-weight: 600;
    }
    .badge-late {
      background: #fdecea;
      color: #c0392b;
    }
    .badge-ok {
      background: #eafaf1;
      color: #1a6e3a;
    }
    .cta {
      display: inline-block;
      padding: 10px 20px;
      background: #1a1a2e;
      color: #ffffff;
      text-decoration: none;
      border-radius: 3px;
      font-size: 13px;
      font-weight: 500;
    }
    .footer {
      padding: 16px 32px;
      background: #f9f9f9;
      border-top: 1px solid #eee;
      font-size: 11px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="header">
      <h1>Morning Attendance Report</h1>
      <p>{{companyName}} &mdash; {{reportDate}}</p>
    </div>

    <div class="body">
      <p>Good morning, <strong>{{managerName}}</strong>. Here is your daily attendance summary.</p>

      {{#if showAllClear}}

      <div class="all-clear">
        <strong>All Clear</strong>
        All scheduled employees have clocked in on time. No action required.
      </div>

      {{else}}

      <div class="summary-row">
        <div class="summary-card">
          <div class="label">Missed Clock-Ins</div>
          <div class="value">{{missedCount}}</div>
        </div>
        <div class="summary-card">
          <div class="label">Currently Clocked In</div>
          <div class="value">{{clockedInCount}}</div>
        </div>
      </div>

      {{#if missedClockIns.length}}
      <div class="section-title">Missed Clock-Ins</div>
      <table class="data-table">
        <thead>
          <tr>
            <th>Employee</th>
            <th>Department</th>
            <th>Scheduled</th>
            <th>Status</th>
            <th>Supervisor</th>
          </tr>
        </thead>
        <tbody>
          {{#each missedClockIns}}
          <tr>
            <td><strong>{{this.employeeName}}</strong></td>
            <td>{{this.department}}</td>
            <td>{{this.scheduledTime}}</td>
            <td><span class="badge badge-late">{{this.minutesLate}} min late</span></td>
            <td>{{this.supervisor}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
      {{/if}}

      {{#if currentlyClockedIn.length}}
      <div class="section-title">Currently Clocked In</div>
      <table class="data-table">
        <thead>
          <tr>
            <th>Employee</th>
            <th>Department</th>
            <th>Clock-In Time</th>
            <th>Hours</th>
          </tr>
        </thead>
        <tbody>
          {{#each currentlyClockedIn}}
          <tr>
            <td><strong>{{this.employeeName}}</strong></td>
            <td>{{this.department}}</td>
            <td>{{this.clockInTime}}</td>
            <td><span class="badge badge-ok">{{this.hoursWorked}} hrs</span></td>
          </tr>
          {{/each}}
        </tbody>
      </table>
      {{/if}}

      {{/if}}

      <a href="{{appUrl}}/dashboard/company-panel/punch-logs" class="cta">View Full Report</a>
    </div>

    <div class="footer">
      This is an automated daily report from BizBuddy. Please do not reply to this email.
      &copy; {{currentYear}} BizBuddy LLC.
    </div>
  </div>
</body>
</html>
